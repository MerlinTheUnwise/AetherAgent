{
  "name": "@withaether/agent",
  "version": "0.1.0",
  "description": "Aether Agent — verified local file automation for AetherAutomate",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/MerlinTheUnwise/AetherAgent.git"
  },
  "bin": {
    "aether-agent": "./dist/index.js"
  },
  "files": [
    "dist",
    "!dist/binaries",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "bundle": "esbuild dist/index.js --bundle --platform=node --target=node18 --format=cjs --outfile=dist/bundle.cjs --external:keytar --external:systray2 --external:node-notifier",
    "build:win": "npx pkg dist/bundle.cjs --target node18-win-x64 --output dist/binaries/AetherAgent.exe --compress GZip",
    "build:mac": "npx pkg dist/bundle.cjs --target node18-macos-x64 --output dist/binaries/AetherAgent-mac --compress GZip",
    "build:linux": "npx pkg dist/bundle.cjs --target node18-linux-x64 --output dist/binaries/aether-agent-linux --compress GZip",
    "build:all": "npm run build && npm run bundle && npm run build:win && npm run build:mac && npm run build:linux",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run"
  },
  "pkg": {
    "assets": [],
    "outputPath": "dist/binaries"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "node-notifier": "^10.0.1",
    "systray2": "^2.1.4",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/node-notifier": "^8.0.5",
    "@types/ws": "^8.5.0",
    "@yao-pkg/pkg": "^6.14.1",
    "esbuild": "^0.27.4",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "vitest": "^4.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
